import Constants from 'expo-constants';

// Récupère les données réelles configurées dans le tableau de bord Expo (extra)
const { 
  expoPublicApiKey, 
  expoPublicGoogleClientId,
  expoPublicBaseUrl 
} = Constants.expoConfig?.extra || {};

// Assignation aux constantes utilisées dans le reste du code
const API_KEY = expoPublicApiKey || process.env.EXPO_PUBLIC_API_KEY;
const GOOGLE_CLIENT_ID = expoPublicGoogleClientId || process.env.EXPO_PUBLIC_GOOGLE_CLIENT_ID;
const WS_BASE = expoPublicBaseUrl || 'https://eueke282zksk1zki18susjdksisk18sj.onrender.com';

const API_BASE = `${WS_BASE}/auth`;
